ENVIRONMENT=development
DATABASE_URL=sqlite:///./data/resolvyn.db
CORS_ORIGINS=["http://localhost:3000"]

# Optional — leave unset to run on deterministic mock outputs (project.md §87)
LLM_API_KEY=
LLM_MODEL=
